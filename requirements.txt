# ── 핵심 프레임워크 ──────────────────────────────────────────────
langgraph>=0.2.0
langchain-core>=0.2.0

# ── LLM API ─────────────────────────────────────────────────────
openai>=1.30.0              # gpt-4o(생성/검증/채점) + gpt-4o-mini(저비용 판단)

# ── 벡터 DB / RAG ───────────────────────────────────────────────
chromadb>=0.5.0             # 로컬 벡터 DB (FAISS로 교체 가능)
rank-bm25>=0.2.2            # 키워드 검색 (하이브리드 RAG)
sentence-transformers>=3.0  # 임베딩 + 크로스인코더 재순위화

# ── 출력 검증 ────────────────────────────────────────────────────
pydantic>=2.0.0             # 형식 붕괴 방지

# ── 백엔드 API ───────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9      # 파일 업로드(강의자료 PDF/녹음/영상) 폼 파싱

# ── PDF 로드 (강의자료 인덱싱) ──────────────────────────────────
pypdf>=4.0.0

# ── 평가 인프라 (MD 섹션 11) ─────────────────────────────────────
ragas>=0.1.0                # RAG 검색 품질 + Faithfulness 측정
deepeval>=1.0.0             # G-Eval 커스텀 채점 품질 기준
langsmith>=0.1.0            # 트레이싱, 골든셋 관리, 버전 비교

# ── 유틸 ────────────────────────────────────────────────────────
python-dotenv>=1.0.0        # .env 로드
tenacity>=8.0.0              # API 호출 재시도 (지수 백오프)
